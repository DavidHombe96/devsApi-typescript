git init 
git status 
git add <namefile>
git add .  <"add all files">
git commint -m "..."
git log


git config --global user.name "name"
git config --global user.email "example@gmail.com"

git diff

git remote add origin htpps://github.com/<seu-usuario>/<seu-repo>.git
git remote -v
git push -u origin main 
git push

git clone <url>

git branch <"Para saber  em que branch se encontramos">
git checkout -b feat1 <"Comando usado para criar   uma nova branch para essa funcionalidade">

git push -u origin feat1 <"Enviar para o a nuven do github ">

git checkout  <"Nome da Branch criada"> <"Comando usado para mudar de Branch">

git pull <"Atualisa os commints">


<!-- Voltando -->
git checkout main
git merge <"Pega os commits que estão na outras branch para o branch principal">
git push 